import 'package:flutter/material.dart';

class AppColors {
  // Primary background: #0A0A0F -> #0F0F1A
  static const Color bgPrimary = Color(0xFF0A0A0F);
  static const Color bgSecondary = Color(0xFF0F0F1A);

  // Surface / cards: #12121C with subtle #1A1A2E borders
  static const Color surfaceColor = Color(0xFF12121C);
  static const Color borderColor = Color(0xFF1A1A2E);

  // Gradient accents
  static const List<Color> electricBlueGradient = [Color(0xFF3B82F6), Color(0xFF1E40AF)];
  static const List<Color> violetGradient = [Color(0xFF8B5CF6), Color(0xFF6D28D9)];
  static const List<Color> cyanGlowGradient = [Color(0xFF06B6D4), Color(0xFF0891B2)];

  // Text
  static const Color textPrimary = Color(0xFFF5F5F7);
  static const Color textSecondary = Color(0xFFA1A1AA);
  static const Color textMuted = Color(0xFF71717A);
}
